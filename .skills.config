# Skills this project adopts from agentic-workflow-template.
# Tokens: group | skill | -group | -skill  (see template/skill-groups.config).
# Sync with:  python <template>/scripts/sync-skills.py <this repo>
core
